# Data Branch - Central Dataset Repository

* This dedicated branch hosts and manages all curated datasets for the AI Chatbot project.
* Structure: `RAG/` (RAG Context), `fine_tune/` (fine-tuning model).
* Maintain metadata fields (`question`, `answer`, `source`) uniformly in JSON file.
* The statistical table of question-answer datasets from the JSON file is saved in `STATISTICS.md`.